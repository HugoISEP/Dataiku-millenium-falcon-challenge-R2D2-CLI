spring:
  main:
    banner-mode: off
    web-application-type: none
  datasource:
    driver-class-name: org.sqlite.JDBC
    url: "jdbc:sqlite::memory:"
  jpa:
    database-platform: com.starwars.millenniumfalcononboardcomputer.configuration.SQLiteDialect
    hibernate.ddl-auto: update
logging:
  level:
    root: OFF
